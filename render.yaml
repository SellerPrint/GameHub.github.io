services:
  - type: web
    name: morpionhub
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true

  # Optionnel : Base de données si vous voulez étendre plus tard
  # - type: pserv
  #   name: morpionhub-db
  #   env: node
  #   plan: free
  #   diskSizeGB: 1
